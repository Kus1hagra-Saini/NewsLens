"""HTTP API routers.

Each module here exposes an APIRouter that `src.main` mounts. Routers are
added week by week per §13; Week 1 leaves them as documented stubs so the
package structure matches the architecture doc and imports do not break.
"""
