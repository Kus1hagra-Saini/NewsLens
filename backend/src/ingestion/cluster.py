"""Story clustering over recent article embeddings.

Groups articles into stories in the `stories` table, populating
articles.story_id. Advances rows from `embedded` to `clustered`.
Implemented in Week 2.
"""
