"""Ingestion pipeline: fetch → extract → embed → cluster → enrich → compare.

Implemented in Week 1 (fetch/extract) through Week 2 (embed/cluster/enrich/
compare) per architecture §13. This package's shape follows §11 exactly.
"""
